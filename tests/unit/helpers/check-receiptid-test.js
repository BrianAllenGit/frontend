
import { checkReceiptid } from 'frontend/helpers/check-receiptid';
import { module, test } from 'qunit';

module('Unit | Helper | check receiptid');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = checkReceiptid([42]);
  assert.ok(result);
});

