<div id="dont-flex">
	{{#unless activeModel}}
		{{infinite-table model=model storeId=storeId loadMore="loadMore" activeModel=activeModel}}
	{{else}}
		{{update-product activeModel=activeModel storeId=storeId}}
	{{/unless}}
</div>
